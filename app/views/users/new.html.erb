<h1>Send login til email</h1>

<%= form_for(@user) do |f| %>
  <!-- TODO: Render errors -->
  <% # render 'shared/errors', object: @user %>
  <p>
    <%= f.label :email %>
    <%= f.text_field :email %>&nbsp;&nbsp;&nbsp;&nbsp;
    <%= f.submit 'Send login link til email' %>
  </p>
<% end %>
